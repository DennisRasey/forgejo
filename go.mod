Intentionally break this so renovate won't update or open a new PR for it. This requires https://codeberg.org/forgejo/forgejo/pulls/12500 to be merged first, as it has some gitlab dependency bumps which resolves the conflict with the gitlab dependency bump from this dependency update